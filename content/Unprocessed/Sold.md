> [!info] Moved
> Cash-sale records are kept local-only for privacy — see `content/private/Sold.md` on this machine (gitignored, never synced, never on the live site). This stub just marks that the "Sold" scenario exists; there's nothing to fill in here.
